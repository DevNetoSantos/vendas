<div id="<%= dom_id category %>">
  <%= category.name %>
  <%= link_to 'Editar', edit_category_path(category) %>
  <%= link_to 'Deletar', category_path(category), data: {
    turbo_method: :delete,
    turbo_confirm: 'Tem certeza deseja excluir categoria?'
    } 
  %>
</div>
